profile

debug

true

false

pi

(quote (a b c d))

(def circle-area (lambda (r) (* pi (* r r))))

(circle-area 3)

(def fact (lambda (n) (if (<= n 1) 1 (* n (fact (- n 1))))))

(fact 10)

(fact 100)

(circle-area (fact 20))

(def twice (lambda (x) (* 2 x)))

(twice 5)

(def repeat (lambda (f) (lambda (x) (f (f x)))))

((repeat twice) 10)

((repeat (repeat twice)) 10)

((repeat (repeat (repeat twice))) 10)

((repeat (repeat (repeat (repeat twice)))) 10)

(def fib (lambda (n) (if (< n 2) 1 (+ (fib (- n 1)) (fib (- n 2))))))

(range 0 10)

(map fib (range 0 10))

(map fib (range 0 20))

(def count (lambda (item L) (if L (+ (equal? item (first L)) (count item (rest L))) 0)))

; (count 0 (list 0 1 2 3 0 0))

(count (quote the) (quote (the more the merrier the bigger the better)))

; (pow 2 16)

